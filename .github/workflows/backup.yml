name: Backup database

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Install the Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Dump the database
        env:
          DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        run: |
          if [ -z "$DB_URL" ]; then
            echo "SUPABASE_DB_URL is empty"
            exit 1
          fi
          mkdir -p backups
          STAMP=$(date +%Y-%m-%d)
          # Structure and rows are separate dumps. Schema alone restores
          # empty tables, which looks like a working backup and is not one.
          supabase db dump --db-url "$DB_URL" -f "backups/schema-$STAMP.sql"
          supabase db dump --db-url "$DB_URL" --data-only -f "backups/data-$STAMP.sql"

          echo "--- row statements captured ---"
          grep -c "INSERT INTO\|COPY public" "backups/data-$STAMP.sql" || true

          gzip -f "backups/schema-$STAMP.sql"
          gzip -f "backups/data-$STAMP.sql"
          ls -lh backups/

      - name: Keep the last 30 days
        run: |
          cd backups
          ls -1t schema-*.sql.gz 2>/dev/null | tail -n +31 | xargs -r rm --
          ls -1t data-*.sql.gz 2>/dev/null | tail -n +31 | xargs -r rm --
          ls -1 *.sql.gz | wc -l

      - name: Commit it
        run: |
          git config user.name "EventScore backup"
          git config user.email "actions@github.com"
          git add backups/
          if git diff --staged --quiet; then
            echo "Nothing changed"
          else
            git commit -m "Database backup $(date +%Y-%m-%d)"
            git push
          fi
